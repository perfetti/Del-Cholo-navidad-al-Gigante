<h1><%= @post.title %></h1>
<small><p><%= @post.created_at.strftime("%b %d. %Y") %></p></small>
<p><%= @post.body %></p>
<%= link_to "Edit Post", edit_post_path %> | <%= link_to "Delete", @post, data: { confirm: 'Are you sure you want to delete?' }, :method => :delete %>

